global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # otel-collector が prometheus exporter で公開する Claude Code メトリクス
  - job_name: claude-code-otel
    static_configs:
      - targets: ["otel-collector:8889"]
